/** Public site and API — map DNS `mipadel.co.uk` → Cloud Run (see Backend/docs/DOMAIN.md). */
export const APP_ORIGIN = "https://mipadel.co.uk";

/** Production API (same host as website). */
export const API_BASE_URL = `${APP_ORIGIN}/api`;
